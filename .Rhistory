# Load necessary packages
pacman::p_load(tidyverse, knitr, here)
# Import the non-communicable diseases data
data_ncd <- read_csv(here("data/burden-of-disease-ncd.csv"))
View(data_ncd)
# Filter the dataset to include only the three countries chosen by your group.
data_ncd_mini <- data_ncd %>%
select(Entity%in%c("Ghana","Cile","Switzerland"))
# Filter the dataset to include only the three countries chosen by your group.
data_ncd_mini <- data_ncd %>%
filter(Entity%in%c("Ghana","Cile","Switzerland"))
data_ncd_mini
View(data_ncd)
# Filter the dataset to include only the three countries chosen by your group.
data_ncd_mini <- data_ncd %>%
filter(Entity%in%c("Ghana","Chile","Switzerland"))
data_ncd_mini
View(data_ncd_mini)
# Here render a table for the DALY burden over time for the three countries.
# You should pivot the data wider to show each country in a separate column.
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(cols="Entity",
names_to="Country",
values_from = "Value")
View(data_ncd_mini)
head(data_ncd_mini)
head(data_ncd_mini)
# Here render a table for the DALY burden over time for the three countries.
# You should pivot the data wider to show each country in a separate column.
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(cols="Entity",
names_to="Country",
values_from = "Value")
data_ncd_long <- data_ncd_mini %>%
pivot_longer(cols = c(`DALYs (Disability-Adjusted…¹`),
names_to = "metric",
values_to = "value")
data_ncd_long <- data_ncd_mini %>%
pivot_longer(cols = 4:ncol(data_ncd_mini),  # assuming first 3 columns are Entity, Code, Year
names_to = "metric",
values_to = "value")
data_ncd_long
data_ncd_mini
# Here render a table for the DALY burden over time for the three countries.
# You should pivot the data wider to show each country in a separate column.
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(
names_from ="Entity",
values_from = "Value")
# Here render a table for the DALY burden over time for the three countries.
# You should pivot the data wider to show each country in a separate column.
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(names_from ="Entity",
values_from = "Value")
data_ncd_mini
# Here render a table for the DALY burden over time for the three countries.
# You should pivot the data wider to show each country in a separate column.
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(names_from ="Entity",
values_from = c(3:4))
data_ncd_wide
# Here render a table for the DALY burden over time for the three countries.
# You should pivot the data wider to show each country in a separate column.
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(names_from ="Entity",
values_from = c(2:4))
data_ncd_wide
data_ncd_mini
# Here render a table for the DALY burden over time for the three countries.
# You should pivot the data wider to show each country in a separate column.
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(names_from =Entity,
values_from = DALYs)
data_ncd_mini
# Here render a table for the DALY burden over time for the three countries.
# You should pivot the data wider to show each country in a separate column.
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(names_from =Entity,
values_from = `DALYs (Disability-Adjusted Life Years) - Non-communicable diseases - Sex: Both - Age: Age-standardized (Rate)`)
data_ncd_wide
View(data_ncd_wide)
# Here render a table for the DALY burden over time for the three countries.
# You should pivot the data wider to show each country in a separate column.
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(names_from =Entity,
values_from = `DALYs (Disability-Adjusted Life Years) - Non-communicable diseases - Sex: Both - Age: Age-standardized (Rate)`) %>%
kable()
data_ncd_wide
# Here render a table for the DALY burden over time for the three countries.
# You should pivot the data wider to show each country in a separate column.
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(names_from =Entity,
values_from = `DALYs (Disability-Adjusted Life Years) - Non-communicable diseases - Sex: Both - Age: Age-standardized (Rate)`) %>%
kable()
data_ncd_wide <- data_ncd_mini %>%
pivot_wider(names_from =Entity,
values_from = `DALYs (Disability-Adjusted Life Years) - Non-communicable diseases - Sex: Both - Age: Age-standardized (Rate)`) %>%
kable()
data_ncd_wide
